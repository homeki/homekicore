machine:
  environment:
    BUILD_META_HOME: /home/ubuntu/buildmeta

dependencies:
  cache_directories:
    - ~/buildmeta
  override:
    - script/install-dependencies.sh

test:
  override:
    - ./gradlew clean jar
